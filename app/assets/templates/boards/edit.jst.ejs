<h1>Rename Board</h1>

<form>
  <label for="board-title">Title</label>
  <input name="board[title]"
         id="board-title"
         value="<%= board.get("title") %>">
  <input type="submit" value="rename Board">
</form>